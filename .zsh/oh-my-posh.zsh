alias omp="oh-my-posh"

# Sadly re-init'ing the theme doesn't seem to work.
# It might be possible to move/rename the files and then reload the shell,
# but that seems like a hassle not worth fighting over.
# alias omp-persistent='eval "$(omp init zsh --config $HOME/.config/oh-my-posh/night-owl.omp.json)" && exec zsh'
# alias omp-transient='eval "$(omp init zsh --config $HOME/.config/oh-my-posh/night-owl-transient.omp.json)" && exec zsh'
